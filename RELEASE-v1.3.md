# MindGit v1.3.0

## 新功能

### ☁️ 云端同步
- 支持 GitHub Gist 多设备同步
- 启动时自动检查云端更新
- 在设置中的“数据 → 云端同步”手动上传或下载
- 数据冲突提示

### ⏯️ 记录开关
- 在设置中的“记录”分组即时启用或停用网页记录
- 停用只影响后续捕获，不删除已有会话

### ⌨️ 快捷键支持
- `/` 或 `Ctrl+K` 打开搜索
- `↑↓` / `JK` 节点导航
- `←→` / `HL` 折叠/展开节点
- `E` / `C` 展开/折叠全部
- `Enter` 打开选中节点
- `?` 显示快捷键帮助

### 🐛 拖拽排序
- 支持拖拽节点调整顺序
- `》` 手柄触发拖拽
- 可移动为子节点或兄弟节点

## 优化

- 深层节点显示优化（超过3层减小缩进）
- 树形区域支持横向滚动
- 重构弹窗状态刷新，减少重复加载和不必要的重绘
- 更新紧凑工作区 UI，修复设置下拉框重复箭头问题
- 添加关于页面和构建/打包脚本

## 安装

下载 [`mindgit-v1.3.0.zip`](https://github.com/RumiaLunnar/mindGit/releases/tag/v1.3.0)，解压后在 Chrome 扩展管理页面加载已解压的扩展。
